import { createBrowserHistory } from "history"; // Cria um histórico para o React Router usar e atualizar só o componente, sem precisar carregar a página inteira de novo.
const history = createBrowserHistory();

export default history;